.navbar {
    display: grid;
    grid-template-columns: 1fr 2fr;
}

.header-title {
    display: flex;
    align-items: center;
    justify-content: center;
}
.header-choose-desktop {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    align-items: center;
    justify-content: center;
}
.header-choose-desktop a {
    color: hsl(0 0% 0%);
    text-decoration: none;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.1rem;
}

.header-choose-mobile {
    display: none;
    align-items: center;
    justify-content: center;
    font-size: var(--h2);
}

.mobile-menu {
    display: grid;
    height: 100vh;
    position: fixed;
    align-items: center;
    justify-content: center;
    z-index: 100;
}
.mobile-menu a {
    color: white;
    text-decoration: none;
    font-size: var(--h3);
}
.mobile-menu i {
    font-size: var(--h2);
    color: white;
}
:root {
    --h1: 2.986rem;
    --h2: 2.488rem;
    --h3: 2.074rem;
    --h4: 1.728rem;
    --h5: 1.44rem;
    --h6: 1.2rem;
    --p: 1rem;
    --s: 0.833rem;
}
@media only screen and (max-width: 600px) {
    .header-choose-desktop {
        display: none;
    }
    .header-choose-mobile {
        display: flex;
    }
    .navbar {
        grid-template-columns: 1fr 1fr;
    }
}
